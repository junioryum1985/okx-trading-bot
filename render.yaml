services:
  - type: web
    name: btc-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: bash start.sh
    healthCheckPath: /
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
